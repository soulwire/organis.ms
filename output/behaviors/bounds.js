// Generated by CoffeeScript 1.3.3
var Bounds;

Bounds = (function() {

  Bounds.WRAP = 'wrap';

  function Bounds(options) {
    core.extend(this, core.defaults(options || {}, {
      enabled: true,
      min: new Vector(0, 0),
      max: new Vector(1000, 1000),
      type: Bounds.WRAP,
      chance: 1.0
    }));
  }

  Bounds.prototype.apply = function(agent) {
    if (!this.enabled || this.chance < 1 && Math.random() > this.chance) {
      return;
    }
    if (agent.x + agent.radius < this.min.x) {
      agent.x = this.max.x + agent.radius;
    } else if (agent.x - agent.radius > this.max.x) {
      agent.x = this.min.x - agent.radius;
    }
    if (agent.y + agent.radius < this.min.y) {
      return agent.y = this.max.y + agent.radius;
    } else if (agent.y - agent.radius > this.max.y) {
      return agent.y = this.min.y - agent.radius;
    }
  };

  return Bounds;

})();
